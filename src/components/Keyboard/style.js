import { StyleSheet } from "react-native";

export function getStyle(){
    return StyleSheet.create({
        view: {
            flex: 1, 
            padding: 20,
            width: '100%',
            flex: 2,
        }
    })
};